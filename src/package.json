{
  "name": "hello-world-js-action",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && rollup --format cjs --input ../out/index.js -o ../dist/index.js",
    "buildByNcc": "tsc && ncc build ../out/index.js --license ../LICENSE -o ../dist"
  },
  "author": "Carson",
  "license": "MIT",
  "devDependencies": {
    "@actions/core": "^1.10.0"
  }
}
